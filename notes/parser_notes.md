what does parsing do?
it turns the csv lines from text to dictionnaries and usable data

why do we skip the header?
because the header is not valid data that you want inside the parsed data

why do we convert power... into int?
because the csv is default in string so the numbers wouldn't be int format but string

why should parse rows return data instead of printing only?
because you want to save the data so that you can use it in the future vs just looking at it

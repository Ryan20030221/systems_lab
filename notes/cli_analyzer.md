what is a CLI argument?
its to make the files input outputs directly from the terminal rather than hardcoding it

why is --input better than editing a hardcoded path?
because its much faster for automation and scaling

what should CLI do vs what should gpu_lab modules do?
ClI should just be the bridge between the inputs and outputs of the gpu_lab tools

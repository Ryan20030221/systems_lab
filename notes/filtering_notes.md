what is filtering?

filtering is taking a subset of data from a dataset that fit a certain criteria

Why should filtering a new list instead of changing the original list?
because you may want to use different types of filters on the original dataset
so its safer to create a new one

why is min_utilization an argument instead of a hardcoded number?
for automation and flexibility so that you don't have to change the code every
time you want to use a different number

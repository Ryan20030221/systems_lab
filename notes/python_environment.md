
what is venv?
venv is for a isolated version of python with specific libraries and python versions, it uses hte Python version you created it with

why did python3 -m venv fail at first?
because venv wasn't initially installed in the linux system

why did sudo install work?
because sudo is administrative privilige and it has permission to install whatever

how do I know pip is using the project environment if you see a (.venv) in the terminal but also if you see the path with it

why should future packages be installed while .venv is active?
because you want to know exactly what libraries are needed for this particular project

what is a summary?
its basically calculated data

why do we calculate the average?
because its the most descriptive number within a set of numbers

what does one pass mean?
one pass means instead of creating 4 functions you create 1 that does the 4 at the same time so instead of O(4n) its On

should a sumamry return data instead of only printing?
because its that data that's going to be fed into the other algorithms that are going to be important for decision making etc.
